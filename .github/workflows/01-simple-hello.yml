name: Simple Hello Workflow

# 트리거: main 브랜치에 푸시될 때 실행
on:
  push:
    branches: [ main ]

# 작업 정의
jobs:
  greet:
    # 러너: Ubuntu Linux 사용
    runs-on: ubuntu-latest
    
    # 단계 정의
    steps:
      - name: Say Hello
        run: echo "Hello, GitHub Actions!"

