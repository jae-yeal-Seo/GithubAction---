# 워크플로우 기본: 트리거, 작업, 단계, 러너

## 📚 목차
1. [워크플로우란?](#워크플로우란)
2. [트리거 (Triggers)](#트리거-triggers)
3. [작업 (Jobs)](#작업-jobs)
4. [단계 (Steps)](#단계-steps)
5. [러너 (Runners)](#러너-runners)
6. [전체 구조 이해하기](#전체-구조-이해하기)
7. [실습 예제](#실습-예제)
8. [연습 문제](#연습-문제)

---

## 워크플로우란?

**워크플로우(Workflow)**는 GitHub Actions에서 자동화된 프로세스를 정의하는 단위입니다. 워크플로우는 YAML 파일로 작성되며, `.github/workflows/` 디렉토리에 저장됩니다.

### 워크플로우의 핵심 구성 요소

```
워크플로우 (Workflow)
├── 트리거 (Trigger) - 언제 실행할지
├── 작업 (Job) - 무엇을 할지
│   └── 단계 (Step) - 어떻게 할지
└── 러너 (Runner) - 어디서 실행할지
```

---

## 트리거 (Triggers)

**트리거**는 워크플로우를 실행시키는 이벤트입니다. "언제 이 워크플로우를 실행할까?"에 대한 답입니다.

### 주요 트리거 유형

1. **Push 이벤트**: 코드가 특정 브랜치에 푸시될 때
2. **Pull Request 이벤트**: PR이 생성, 업데이트, 머지될 때
3. **Schedule**: 크론 표현식을 사용한 예약 실행
4. **Workflow Dispatch**: 수동으로 실행
5. **다른 워크플로우 완료**: 다른 워크플로우가 끝날 때

### 트리거 예제

```yaml
# push 이벤트로 트리거
on:
  push:
    branches: [ main, develop ]

# pull_request 이벤트로 트리거
on:
  pull_request:
    branches: [ main ]

# 여러 이벤트 조합
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '0 0 * * *'  # 매일 자정에 실행
  workflow_dispatch:  # 수동 실행 가능
```

---

## 작업 (Jobs)

**작업(Job)**은 워크플로우 내에서 실행되는 작업 단위입니다. 하나의 워크플로우는 여러 작업을 포함할 수 있으며, 기본적으로 작업들은 병렬로 실행됩니다.

### 작업의 특징

- 각 작업은 독립적인 러너에서 실행됩니다
- 작업 간 데이터 공유는 아티팩트(artifacts)를 통해 이루어집니다
- 작업은 순차적으로 실행되도록 설정할 수 있습니다 (`needs` 키워드 사용)

### 작업 구조

```yaml
jobs:
  job-name:  # 작업 이름 (고유해야 함)
    runs-on: ubuntu-latest  # 러너 지정
    steps:  # 이 작업의 단계들
      - name: Step 1
        run: echo "Hello"
```

---

## 단계 (Steps)

**단계(Step)**는 작업 내에서 실행되는 개별 명령어나 액션입니다. 각 단계는 순차적으로 실행됩니다.

### 단계의 두 가지 유형

1. **액션 사용**: 재사용 가능한 코드 조각
2. **명령어 실행**: 직접 스크립트 실행

### 단계 예제

```yaml
steps:
  # 액션 사용 예제
  - name: Checkout code
    uses: actions/checkout@v3
  
  # 명령어 실행 예제
  - name: Print message
    run: echo "Hello, GitHub Actions!"
  
  # 여러 명령어 실행
  - name: Setup and build
    run: |
      echo "Setting up..."
      npm install
      npm run build
```

---

## 러너 (Runners)

**러너(Runner)**는 워크플로우가 실행되는 서버입니다. GitHub에서 제공하는 호스팅 러너와 직접 관리하는 셀프 호스팅 러너가 있습니다.

### GitHub 호스팅 러너

- **ubuntu-latest**: Ubuntu Linux (가장 많이 사용)
- **windows-latest**: Windows Server
- **macos-latest**: macOS

### 러너 지정 방법

```yaml
jobs:
  build:
    runs-on: ubuntu-latest  # Ubuntu 러너 사용
  
  test-windows:
    runs-on: windows-latest  # Windows 러너 사용
  
  test-macos:
    runs-on: macos-latest  # macOS 러너 사용
```

---

## 전체 구조 이해하기

### 기본 워크플로우 구조

```yaml
name: My First Workflow  # 워크플로우 이름

on:  # 트리거 정의
  push:
    branches: [ main ]

jobs:  # 작업 정의
  my-job:  # 작업 이름
    runs-on: ubuntu-latest  # 러너 지정
    
    steps:  # 단계 정의
      - name: Step 1
        run: echo "First step"
      
      - name: Step 2
        run: echo "Second step"
```

### 구조 설명

1. **name**: 워크플로우의 이름 (선택사항)
2. **on**: 워크플로우를 실행할 이벤트 정의
3. **jobs**: 실행할 작업들의 집합
4. **runs-on**: 작업이 실행될 러너 지정
5. **steps**: 작업 내에서 실행할 단계들

---

## 실습 예제

### 예제 1: 가장 간단한 워크플로우

이 워크플로우는 코드가 main 브랜치에 푸시될 때마다 간단한 메시지를 출력합니다.

```yaml
name: Simple Hello Workflow

on:
  push:
    branches: [ main ]

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Hello, GitHub Actions!"
```

**실행 방법:**
1. `.github/workflows/hello.yml` 파일 생성
2. 위 내용을 복사하여 붙여넣기
3. 커밋하고 main 브랜치에 푸시
4. GitHub 저장소의 Actions 탭에서 실행 확인

---

### 예제 2: 여러 단계가 있는 워크플로우

이 예제는 여러 단계를 순차적으로 실행하는 워크플로우입니다.

```yaml
name: Multi-Step Workflow

on:
  push:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
      # 1단계: 코드 체크아웃
      - name: Checkout code
        uses: actions/checkout@v3
      
      # 2단계: 환경 정보 출력
      - name: Show environment
        run: |
          echo "Running on: ${{ runner.os }}"
          echo "Node version: $(node --version)"
          echo "Current directory: $(pwd)"
      
      # 3단계: 파일 목록 출력
      - name: List files
        run: ls -la
      
      # 4단계: 완료 메시지
      - name: Complete
        run: echo "All steps completed!"
```

---

### 예제 3: 여러 작업이 있는 워크플로우

이 예제는 여러 작업을 병렬로 실행하는 워크플로우입니다.

```yaml
name: Multiple Jobs Workflow

on:
  push:
    branches: [ main ]

jobs:
  # 작업 1: Linux에서 실행
  job-linux:
    runs-on: ubuntu-latest
    steps:
      - name: Linux Job
        run: |
          echo "Running on Linux"
          uname -a
  
  # 작업 2: Windows에서 실행
  job-windows:
    runs-on: windows-latest
    steps:
      - name: Windows Job
        run: |
          echo "Running on Windows"
          echo %OS%
  
  # 작업 3: macOS에서 실행
  job-macos:
    runs-on: macos-latest
    steps:
      - name: macOS Job
        run: |
          echo "Running on macOS"
          sw_vers
```

**참고**: 위 세 작업은 병렬로 실행됩니다.

---

### 예제 4: 순차 실행 작업

이 예제는 작업들이 순차적으로 실행되도록 설정합니다.

```yaml
name: Sequential Jobs Workflow

on:
  push:
    branches: [ main ]

jobs:
  # 첫 번째 작업
  setup:
    runs-on: ubuntu-latest
    steps:
      - name: Setup
        run: echo "Setting up environment..."
  
  # 두 번째 작업 (setup 완료 후 실행)
  build:
    needs: setup  # setup 작업이 완료되어야 실행
    runs-on: ubuntu-latest
    steps:
      - name: Build
        run: echo "Building application..."
  
  # 세 번째 작업 (build 완료 후 실행)
  deploy:
    needs: build  # build 작업이 완료되어야 실행
    runs-on: ubuntu-latest
    steps:
      - name: Deploy
        run: echo "Deploying application..."
```

**실행 순서**: setup → build → deploy

---

------------------------여기부터------------------------

### 예제 5: 수동 실행 워크플로우

이 예제는 GitHub 웹 인터페이스에서 수동으로 실행할 수 있는 워크플로우입니다.

```yaml
name: Manual Workflow

on:
  workflow_dispatch:  # 수동 실행 트리거
    inputs:  # 사용자 입력 받기
      environment:
        description: 'Deployment environment'
        required: true
        default: 'staging'
        type: choice
        options:
          - staging
          - production
      message:
        description: 'Deployment message'
        required: false
        default: 'Manual deployment'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Show inputs
        run: |
          echo "Environment: ${{ inputs.environment }}"
          echo "Message: ${{ inputs.message }}"
      
      - name: Deploy
        run: |
          echo "Deploying to ${{ inputs.environment }}..."
          echo "${{ inputs.message }}"
```

**사용 방법:**
1. GitHub 저장소의 Actions 탭으로 이동
2. "Manual Workflow" 선택
3. "Run workflow" 버튼 클릭
4. 입력값 선택 후 실행

---

## 연습 문제

### 문제 1: 기본 워크플로우 만들기
다음 요구사항을 만족하는 워크플로우를 작성하세요:
- main 브랜치에 푸시될 때 실행
- Ubuntu 러너 사용
- "Hello World" 메시지 출력
- 현재 날짜와 시간 출력

<details>
<summary>정답 보기</summary>

```yaml
name: Exercise 1

on:
  push:
    branches: [ main ]

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Hello World
        run: echo "Hello World"
      
      - name: Show date
        run: echo "Current date: $(date)"
```
</details>

---

### 문제 2: 여러 단계 워크플로우
다음 단계들을 포함하는 워크플로우를 작성하세요:
1. 코드 체크아웃
2. 현재 디렉토리 출력
3. 파일 목록 출력
4. 완료 메시지 출력

<details>
<summary>정답 보기</summary>

```yaml
name: Exercise 2

on:
  push:
    branches: [ main ]

jobs:
  multi-step:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Show directory
        run: echo "Current directory: $(pwd)"
      
      - name: List files
        run: ls -la
      
      - name: Complete
        run: echo "All steps completed!"
```
</details>

---

### 문제 3: 순차 작업 워크플로우
다음 세 작업을 순차적으로 실행하는 워크플로우를 작성하세요:
1. `prepare` 작업: "Preparing..." 메시지 출력
2. `process` 작업: "Processing..." 메시지 출력 (prepare 완료 후)
3. `finish` 작업: "Finished!" 메시지 출력 (process 완료 후)

<details>
<summary>정답 보기</summary>

```yaml
name: Exercise 3

on:
  push:
    branches: [ main ]

jobs:
  prepare:
    runs-on: ubuntu-latest
    steps:
      - name: Prepare
        run: echo "Preparing..."
  
  process:
    needs: prepare
    runs-on: ubuntu-latest
    steps:
      - name: Process
        run: echo "Processing..."
  
  finish:
    needs: process
    runs-on: ubuntu-latest
    steps:
      - name: Finish
        run: echo "Finished!"
```
</details>

---

## 📖 핵심 정리

### 트리거 (Triggers)
- **역할**: 워크플로우를 실행시키는 이벤트
- **예시**: push, pull_request, schedule, workflow_dispatch
- **위치**: 워크플로우 파일의 `on:` 섹션

### 작업 (Jobs)
- **역할**: 워크플로우 내의 실행 단위
- **특징**: 기본적으로 병렬 실행, `needs`로 순차 실행 가능
- **위치**: 워크플로우 파일의 `jobs:` 섹션

### 단계 (Steps)
- **역할**: 작업 내에서 실행되는 개별 명령어
- **유형**: 액션 사용 (`uses`) 또는 명령어 실행 (`run`)
- **위치**: 작업 내의 `steps:` 섹션

### 러너 (Runners)
- **역할**: 워크플로우가 실행되는 서버
- **종류**: ubuntu-latest, windows-latest, macos-latest
- **위치**: 작업의 `runs-on:` 키워드

---

## 🔗 참고 자료

- [GitHub Actions 공식 문서](https://docs.github.com/en/actions)
- [워크플로우 문법 참조](https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions)
- [러너 정보](https://docs.github.com/en/actions/using-github-hosted-runners/about-github-hosted-runners)

---

## 다음 단계

이제 워크플로우의 기본 구조를 이해했으니, 다음 주제로 넘어가세요:
- **GitHub Actions용 YAML 문법** 심화 학습
- **이벤트와 트리거** 상세 학습
- **작업과 단계 구성** 고급 기법

